(function() {
  "use strict";

  // App.ApplicationAdapter = DS.ActiveModelAdapter.extend({
  //   namespace: '<%= File.join(Spree::Core::Engine.routes.url_helpers.root_path, "api").gsub(/^\//, "") %>'
  // });

  App.ApplicationAdapter = DS.FixtureAdapter.extend();

})();
